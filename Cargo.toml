[package]
name = "mc"
version = "0.1.0"
edition = "2021"

[dependencies]
rand = { version = "0.8.4", features = ["small_rng"] }
rayon = "1.5.1"
serde = "1.0"
serde_derive = "1.0"
toml = "0.5"

[features]
simd = []
